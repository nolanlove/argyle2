{
  "name": "argyle-mobile",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Mobile-first rewrite of argyletheory.com — React 19 + TypeScript. Served by Django at /m/. Desktop at / is frozen and untouched.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "tone": "^14.7.77"
  },
  "devDependencies": {
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
